MAILTO="danial"
*/5 * * * * danial cd /srv/www.tigris.id.au && git checkout master -q && git reset --hard -q && git pull -q origin master >/dev/null
0 * * * * root /usr/sbin/ntpdate ntp.ubuntu.com > /dev/null
